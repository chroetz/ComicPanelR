\documentclass{article}
\usepackage[papersize={~wTot~cm,~hTot~cm},margin=0pt]{geometry}
\usepackage{fontspec}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning}
\pagestyle{empty}
\setlength{\parskip}{0pt}
\setlength{\topskip}{0pt}
\setlength{\parindent}{0pt}
\setmainfont{~font~}

\definecolor{colTextBoxBg}{HTML}{FFE97F}
\definecolor{colTextComputer}{HTML}{4CFF00}

\def\bleed{~bleed~}
\def\widthCm{~wInner~}
\def\heightCm{~hInner~}

\tikzset{
	narration/.style={
		draw = black,
		line width = 0.5mm,
		text = black,
		fill = colTextBoxBg,
		font = \setmainfont{~font~}\fontsize{~fontSizePt~pt}{~lineHeightPt~pt}\selectfont,
	},
	computer/.style={
		text = colTextComputer,
		font = \setmainfont{Courier New}\fontsize{8pt}{10pt}\selectfont,
	},
	speechText/.style={
		text = black,
		font = \setmainfont{~font~}\fontsize{~fontSizePt~pt}{~lineHeightPt~pt}\selectfont,
		inner sep=0pt,
	},
	speechEllipse/.style={
		draw = black,
		fill = white,
		line width = 0.5mm,
	},
	textArea/.style={
		draw = none,
		fill = none%red
	},
	whisperText/.style={
		text = black,
		font = \setmainfont{~font~}\fontsize{~fontSizePt~pt}{~lineHeightPt~pt}\selectfont,
		inner sep=0pt,
	},
	whisperEllipse/.style={
		draw = black,
		fill = white,
		line width = 0.5mm,
		dashed,
	},
	thoughtText/.style={
		text = black,
		font = \setmainfont{~font~}\fontsize{~fontSizePt~pt}{~lineHeightPt~pt}\selectfont,
		inner sep=0pt,
	},
	thoughtEllipse/.style={
		draw = black,
		fill = white,
		line width = 0.5mm,
	},
}

\begin{document}%
	\fontsize{~fontSizePt~pt}{~lineHeightPt~pt}\selectfont
	\begin{tikzpicture}[remember picture, overlay]%
		\node at (current page.center) {\includegraphics[width=\paperwidth,height=\paperheight]{~imageBelow~}};
	\end{tikzpicture}%
	\begin{tikzpicture}[remember picture, overlay, y=-1cm, shift={(\bleed cm, -\bleed cm)}]%
		~main~
	\end{tikzpicture}%
	\begin{tikzpicture}[remember picture, overlay]%
		\node at (current page.center) {\includegraphics[width=\paperwidth,height=\paperheight]{~imageAbove~}};
	\end{tikzpicture}%
%	\begin{tikzpicture}[remember picture, overlay, y=-1cm, shift={(0.3cm, -0.3cm)}]%
%		\fill[red] (-\bleed, -\bleed) rectangle ++(\bleed, \heightCm+2*\bleed);
%		\fill[red] (-\bleed, -\bleed) rectangle ++(\widthCm+2*\bleed, \bleed);
%		\fill[red] (-\bleed, \heightCm) rectangle ++(\widthCm+2*\bleed, \bleed);
%		\fill[red] (\widthCm, -\bleed) rectangle ++(\bleed, \heightCm+2*\bleed);
%		\draw[help lines,xstep=1cm,ystep=1cm] (0,0) grid (\widthCm,\heightCm);
%		\foreach \x in {0,1,...,\widthCm} {
%			\foreach \y in {0,1,...,\heightCm} {
%				\node [anchor=north west] at (\x,\y) {{\tiny\x,\y}}; 
%			}
%		}
%	\end{tikzpicture}%
\end{document}
